The  folders contain the following Data:

1) CSVParser.ipynb : The CSV Parser is used to extract data from csv and write to txt files in a separate folder.

2) FilePrePRocessor.ipynb : The File preprocessor is the widely used file that processes data which results in text files that is fed to MR as input.

3) mapperCoTw.py : Code specifically written to find word Co-occurrence in Twitter Data.